import * as React from "react";

/*
describe("Get Menu Data", function() {

  beforeEach(()=> {
    FabaCore.addMediator(new MenuMediator());
  });

  it("Mediator should be added", function() {
    expect(FabaCore.mediators.length).toBe(1);
  });

  it("MeuData should return offline data", function(resolve) {
    new GetMenuDataEvent().dispatch(function(){
      resolve();
    });
  });
});
  */
